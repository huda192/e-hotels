/**
  create index ix_hotel_name_address on hotel (hotelname, hoteladdress);
  update hotel set hotelname = hotelname;
**/

/**
  create index ix_employee_fname_lname on employee (empfirstname, emplastname);
  update employee set empfirstname = empfirstname;
**/

/**
  create index ix_room_price_capacity on room (price, capacity);
  update room set price = price;
**/
